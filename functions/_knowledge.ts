// STUB — replaced by `npm run build-knowledge`.
// Until you run that script, the assistant will refuse every question.

export const KNOWLEDGE_BASE: string =
  "(No source documents loaded. Run `npm run build-knowledge` from the project root to ingest the training corpus.)";
